// =======================================================================
// Test Setup
// =======================================================================
var expect = require('chai').expect,
	assert = require('chai').assert,
	path = require('path'),
	appRoot = process.env.PWD,
	libDir = appRoot + path.sep + 'lib' + path.sep;

// =======================================================================
// nGenerate Tests
// =======================================================================
describe('Module generator command line interface', function() {

	

});